{
  "name": "mango",
  "private": true,
  "type": "module",
  "workspaces": [
    "packages/*",
    "website"
  ],
  "scripts": {
    "version": "lerna version prerelease --preid alpha --no-private --no-push --no-git-tag-version --no-commit-hooks",
    "prepublishOnly": "node tools/build.js",
    "publish": "lerna publish from-package --no-private --no-push --no-git-tag-version"
  },
  "devDependencies": {
    "esbuild": "^0.18.2",
    "lerna": "^7.0.1",
    "nx": "^16.3.2",
    "terser": "^5.18.0"
  },
  "engines": {
    "node": ">=16.0.0"
  }
}
